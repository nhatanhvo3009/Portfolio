# Git hub instruction

## Upload to github
After making any changes, do the following:
```bash
git add -A # this adds all changes
git commit -m "<your message>" # message must be descriptive for future reverts
git push
```

## Download from github
When change from other device, do the following:
```bash
git pull
```